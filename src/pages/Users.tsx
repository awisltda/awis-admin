import { Card } from '../ui/Card'

export function Users() {
  return (
    <Card title="Usuários" subtitle="Usuários AWIS e administração global.">
      <div className="awis-muted">
        Próximo passo: CRUD de usuários (AWIS/ADM) e atribuição de roles.
      </div>
    </Card>
  )
}
